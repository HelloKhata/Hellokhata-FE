# ERP backend base URL. Include its /api prefix.
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Client rollout gate only. The ERP backend remains authoritative.
NEXT_PUBLIC_AI_ENABLED=false

# Platform Admin is a separate security domain from shop/tenant sessions.
NEXT_PUBLIC_PLATFORM_ADMIN_ENABLED=false
